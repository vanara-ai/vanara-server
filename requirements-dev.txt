# Development dependencies for Vanara.ai backend.
#
# Install alongside runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Runtime deps stay in requirements.txt to keep production images lean.

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.6.0
pre-commit>=3.6.0
